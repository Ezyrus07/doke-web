name: Archived SCHED-B04C Bigint Reconciler

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary reconciler archived; no execution permitted."

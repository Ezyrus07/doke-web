name: TEMP COM-B02AK Matrix Finalizer

on:
  pull_request:
    branches: [rep/rep-001-baseline-audit]
    paths:
      - '.github/workflows/temp-com-b02ak-matrix-finalizer.yml'

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: temp-com-b02ak-matrix-finalizer-${{ github.ref }}
  cancel-in-progress: false

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Prove exact temporary lineage and frozen Matrix source
        shell: bash
        run: |
          set -euo pipefail
          BASE=102c9b93b51f7c2062fd2f051e5a8672751c3b26
          CANDIDATE=30436eefd7236b108b5dbaa5e69a4f61c4b3720f
          TEMP=.github/workflows/temp-com-b02ak-matrix-finalizer.yml
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          test "$(git rev-parse HEAD^)" = "$CANDIDATE"
          test "$(git rev-parse HEAD:config/domain-completion-matrix.json)" = "b9b2abbee1aed1033c122bf7f2802c7e80844623"
          test "$(git diff --name-only "$CANDIDATE" HEAD)" = "$TEMP"
          expected_candidate="$(printf '%s\n' \
            .github/workflows/com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.yml \
            backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-readiness.js \
            config/com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.json \
            scripts/test-com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.js | sort)"
          actual_candidate="$(git diff --name-only "$BASE" "$CANDIDATE" | sort)"
          test "$actual_candidate" = "$expected_candidate"

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Generate canonical Matrix derivatives only
        shell: bash
        run: |
          set -euo pipefail
          SOURCE_BEFORE="$(git rev-parse HEAD:config/domain-completion-matrix.json)"
          test "$SOURCE_BEFORE" = "b9b2abbee1aed1033c122bf7f2802c7e80844623"
          npm run write:domain-completion-matrix
          git diff --exit-code -- config/domain-completion-matrix.json
          expected="$(printf '%s\n' docs/DOMAIN-COMPLETION-MATRIX.md reports/generated/domain-completion-matrix-report.json | sort)"
          actual="$(git diff --name-only HEAD | sort)"
          test "$actual" = "$expected"

      - name: Revalidate B02AK Matrix governance and diff hygiene
        shell: bash
        run: |
          set -euo pipefail
          node --check backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-readiness.js
          node --check scripts/test-com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.js
          node scripts/test-com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.js
          TARGET=backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-readiness.js
          ! grep -E 'new (Map|WeakMap|Set|WeakSet)\(|randomUUID\(|randomBytes\(|crypto\.' "$TARGET"
          ! grep -E 'createClient\(|\.rpc\(|\.from\(|fetch\(|process\.env|SUPABASE_SERVICE_ROLE_KEY' "$TARGET"
          ! grep -E '\.(registerOpaqueContinuationState|resolveOpaqueContinuationState|releaseOpaqueContinuationState)\(' "$TARGET"
          ! grep -E 'storageBackendMaterialized[[:space:]]*:[[:space:]]*true|entryContainerMaterialized[[:space:]]*:[[:space:]]*true|operationMethodsAttachedToInstance[[:space:]]*:[[:space:]]*true|continuationStateStored[[:space:]]*:[[:space:]]*true' "$TARGET"
          ! grep -E 'registryOperationInvoked[[:space:]]*:[[:space:]]*true|registryLookupExecuted[[:space:]]*:[[:space:]]*true|registryReleaseExecuted[[:space:]]*:[[:space:]]*true' "$TARGET"
          npm run audit:domain-completion-matrix
          npm run audit:agent-governance
          git diff --check

      - name: Self-remove TEMP and prove permanent B02AK scope
        shell: bash
        run: |
          set -euo pipefail
          BASE=102c9b93b51f7c2062fd2f051e5a8672751c3b26
          TEMP=.github/workflows/temp-com-b02ak-matrix-finalizer.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- docs/DOMAIN-COMPLETION-MATRIX.md
          git add -f -- reports/generated/domain-completion-matrix-report.json
          git rm -- "$TEMP"
          expected_staged="$(printf '%s\n' \
            .github/workflows/temp-com-b02ak-matrix-finalizer.yml \
            docs/DOMAIN-COMPLETION-MATRIX.md \
            reports/generated/domain-completion-matrix-report.json | sort)"
          actual_staged="$(git diff --cached --name-only | sort)"
          test "$actual_staged" = "$expected_staged"
          git diff --cached --check
          git commit -m "docs(com): reconcile B02AK Domain Completion Matrix derivatives"
          test "$(git rev-parse HEAD:config/domain-completion-matrix.json)" = "b9b2abbee1aed1033c122bf7f2802c7e80844623"
          expected_permanent="$(printf '%s\n' \
            .github/workflows/com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.yml \
            backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-readiness.js \
            config/com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.json \
            docs/DOMAIN-COMPLETION-MATRIX.md \
            reports/generated/domain-completion-matrix-report.json \
            scripts/test-com-b02ak-repository-only-continuation-state-registry-storage-backend-readiness.js | sort)"
          actual_permanent="$(git diff --name-only "$BASE" HEAD | sort)"
          test "$actual_permanent" = "$expected_permanent"
          ! git diff --name-only "$BASE" HEAD | grep '^supabase/migrations/'

      - name: Fast-forward branch only
        shell: bash
        run: |
          set -euo pipefail
          TEMP_HEAD="${{ github.event.pull_request.head.sha }}"
          git fetch origin com/com-001-baseline-audit
          test "$(git rev-parse origin/com/com-001-baseline-audit)" = "$TEMP_HEAD"
          git push origin HEAD:com/com-001-baseline-audit

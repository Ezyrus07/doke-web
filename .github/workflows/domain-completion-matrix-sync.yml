name: Domain Completion Matrix Artifact Sync

on:
  push:
    branches: [com/com-001-baseline-audit]
    paths:
      - '.github/workflows/domain-completion-matrix-sync.yml'

permissions: { contents: write }

jobs:
  sync:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Pre-writer R4H repository-only gates
        shell: bash
        run: |
          node --check backend/modules/communities/community-realtime-private-auth-r4h.js
          node --check scripts/test-com-b03c-r4h-presence-only-terminal-observation-authorization-lifecycle-readiness.js
          node scripts/test-com-b03c-r4h-presence-only-terminal-observation-authorization-lifecycle-readiness.js
          test ! -e config/com-b03c-r4h-single-use-hosted-terminal-status-observation-authorization-trigger.json
          node - <<'NODE'
          const r4h = require('./backend/modules/communities/community-realtime-private-auth-r4h');
          const config = require('./config/com-b03c-r4h-presence-only-terminal-observation-authorization-lifecycle-readiness.json');
          const result = r4h.evaluateRepositoryReadiness(config.readinessInput);
          if (result.decision !== r4h.STATUS || result.authorizationLifecycleReady !== true) process.exit(1);
          for (const field of [
            'triggerCreationAuthority','remoteExecutionAuthority','remoteCredentialReadAuthority',
            'remoteDependencyLoadAuthority','networkAuthority','stagingReadAuthority',
            'stagingMutationAuthority','realtimeSubscriptionAuthority','authIdentityLifecycleAuthority',
            'runtimeChangeAuthority','productionAuthority','pullRequestMergeAuthority'
          ]) if (result[field] !== false) process.exit(1);
          if (result.authorizationReceived !== false || result.authorizationConsumed !== false) process.exit(1);
          NODE
          R4G_HEAD=16b7da14ea41e8f994a045b59493fc2e13ad65b6
          test -z "$(git diff --name-only "$R4G_HEAD" HEAD -- \
            backend/modules/communities/community-realtime-private-auth-r4g.js \
            scripts/execute-com-b03c-r4g-presence-only-terminal-observation-envelope.js \
            scripts/execute-com-b03c-r3g-remote-adapter-staging-diagnostic.js \
            scripts/execute-com-b03c-r3v-single-use-remote-execution-envelope.js)"
      - name: Canonical writer
        run: npm run write:domain-completion-matrix
      - name: Restrict canonical writer outputs
        shell: bash
        run: |
          mapfile -t changed < <(git diff --name-only)
          test "${#changed[@]}" -eq 2
          printf '%s\n' "${changed[@]}" | grep -Fx 'docs/DOMAIN-COMPLETION-MATRIX.md'
          printf '%s\n' "${changed[@]}" | grep -Fx 'reports/generated/domain-completion-matrix-report.json'
      - name: Canonical matrix audit
        run: npm run audit:domain-completion-matrix
      - name: Preserve R4H repository guards
        shell: bash
        run: |
          node scripts/test-com-b03c-r4h-presence-only-terminal-observation-authorization-lifecycle-readiness.js
          test ! -e config/com-b03c-r4h-single-use-hosted-terminal-status-observation-authorization-trigger.json
          node - <<'NODE'
          const r4h = require('./backend/modules/communities/community-realtime-private-auth-r4h');
          const config = require('./config/com-b03c-r4h-presence-only-terminal-observation-authorization-lifecycle-readiness.json');
          const result = r4h.evaluateRepositoryReadiness(config.readinessInput);
          if (result.authorizationLifecycleReady !== true || result.freshAuthorizationRequired !== true) process.exit(1);
          if (result.remoteExecutionAuthority !== false || result.stagingReadAuthority !== false || result.stagingMutationAuthority !== false) process.exit(1);
          if (result.productionAuthority !== false || result.pullRequestMergeAuthority !== false) process.exit(1);
          if (result.exactRootCauseProven !== false || result.causalPromotionAllowed !== false) process.exit(1);
          NODE
      - name: Agent Governance
        run: npm run audit:agent-governance
      - name: Contain governance-generated side effects
        shell: bash
        run: |
          mkdir -p /tmp/r4h-matrix
          cp docs/DOMAIN-COMPLETION-MATRIX.md /tmp/r4h-matrix/DOMAIN-COMPLETION-MATRIX.md
          cp reports/generated/domain-completion-matrix-report.json /tmp/r4h-matrix/domain-completion-matrix-report.json
          git reset --hard HEAD
          git clean -fd
          cp /tmp/r4h-matrix/DOMAIN-COMPLETION-MATRIX.md docs/DOMAIN-COMPLETION-MATRIX.md
          mkdir -p reports/generated
          cp /tmp/r4h-matrix/domain-completion-matrix-report.json reports/generated/domain-completion-matrix-report.json
      - name: Stage exactly two canonical outputs
        shell: bash
        run: |
          mapfile -t changed < <(git diff --name-only)
          test "${#changed[@]}" -eq 2
          printf '%s\n' "${changed[@]}" | grep -Fx 'docs/DOMAIN-COMPLETION-MATRIX.md'
          printf '%s\n' "${changed[@]}" | grep -Fx 'reports/generated/domain-completion-matrix-report.json'
          git add docs/DOMAIN-COMPLETION-MATRIX.md
          git add -f reports/generated/domain-completion-matrix-report.json
          mapfile -t staged < <(git diff --name-only --cached)
          test "${#staged[@]}" -eq 2
          printf '%s\n' "${staged[@]}" | grep -Fx 'docs/DOMAIN-COMPLETION-MATRIX.md'
          printf '%s\n' "${staged[@]}" | grep -Fx 'reports/generated/domain-completion-matrix-report.json'
      - name: Commit canonical R4H-derived outputs
        shell: bash
        run: |
          git config user.name doke
          git config user.email bielantoniomma@gmail.com
          git commit -m 'docs(com): reconcile domain matrix after R4H authorization boundary'
          git push origin HEAD:com/com-001-baseline-audit

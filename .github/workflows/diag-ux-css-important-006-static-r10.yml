name: Doke UX CSS Important 006 Static R10
on:
  push:
    branches: [diag/ux-css-important-006-static-r10]
    paths: [.github/workflows/diag-ux-css-important-006-static-r10.yml]
permissions: {contents: write}
jobs:
  prove:
    runs-on: ubuntu-24.04
    env:
      TARGET_SHA: 24b49e5693f24621a1024b94c33c8ba6e18b12c0
      TARGET_FILE: assets/css/pages/home/mobile-composition.css
      TARGET_BLOB: 896730319ae0852caca8a0f93cd1507608dd9894
      DIAG_BRANCH: diag/ux-css-important-006-static-r10
      RESULT_FILE: .diagnostics/ux-css-important-006-static-r10.txt
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with: {fetch-depth: 0}
      - id: prove
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail; exec > >(tee /tmp/prove.log) 2>&1
          test "$(git rev-parse HEAD^)" = "$TARGET_SHA"; git checkout "$TARGET_SHA" -- .; test "$(git hash-object "$TARGET_FILE")" = "$TARGET_BLOB"
          node <<'NODE'
          const fs=require('fs'),crypto=require('crypto'),f=process.env.TARGET_FILE,esc=x=>x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');
          const parent=fs.readFileSync(f,'utf8');if((parent.match(/!important\b/g)||[]).length!==23)throw Error('parent drift');let s=parent.replace(/\s*!important\b/g,'');
          function restore(selector,props){const re=new RegExp(esc(selector)+'\\s*\\{','g'),all=[...s.matchAll(re)].map(m=>{const b=s.indexOf('{',m.index),e=s.indexOf('}',b);return{b,e,block:s.slice(b+1,e)}}),hits=all.filter(x=>props.every(p=>new RegExp(`(^|\\n)\\s*${esc(p)}\\s*:`,'m').test(x.block)));if(hits.length!==1)throw Error(`restore ${selector} hits=${hits.length}`);let block=hits[0].block;for(const p of props){block=block.replace(new RegExp(`(^|\\n)(\\s*${esc(p)}\\s*:[^;]+);`,'m'),'$1$2 !important;')}s=s.slice(0,hits[0].b+1)+block+s.slice(hits[0].e)}
          restore('body.home-index-shell .featured-services,\n  body.home-index-shell .before-after,\n  body.home-index-shell .short-videos',['overflow']);restore('body.home-index-shell .doke-search-panel__form',['position','overflow']);restore('body.home-index-shell .home-search-hero__field::before,\n  body.home-index-shell .home-search-hero__field::after',['display']);restore('body.home-index-shell .featured-services .content-rail',['overflow']);if((s.match(/!important\b/g)||[]).length!==5)throw Error('candidate count');const z=Buffer.from(s),h=crypto.createHash('sha256').update(z).digest('hex');console.log(`CANDIDATE IDENTITY|sha256=${h}|bytes=${z.length}|important=23->5|priorityMarkersRemoved=18|searchMarkersPreserved=3|sectionOverflowPreserved=1|featuredOverflowGuardPreserved=1`);
          const clean=x=>x.replace(/\/\*[\s\S]*?\*\//g,'');function close(x,b){let d=0;for(let i=b;i<x.length;i++){if(x[i]==='{')d++;else if(x[i]==='}'&&--d===0)return i}throw Error('brace')}function parse(x,ctx=[],out=[]){x=clean(x);let i=0;while(i<x.length){while(i<x.length&&/\s/.test(x[i]))i++;if(i>=x.length)break;let b=x.indexOf('{',i),semi=x.indexOf(';',i);if(semi>=0&&(b<0||semi<b)){i=semi+1;continue}if(b<0)break;const head=x.slice(i,b).trim(),e=close(x,b),body=x.slice(b+1,e);if(head.startsWith('@media'))parse(body,[...ctx,head.replace(/\s+/g,' ')],out);else out.push({selectors:head.split(',').map(v=>v.trim().replace(/\s+/g,' ')),body,order:out.length});i=e+1}return out}function dec(body,p){const m=body.match(new RegExp(`(?:^|;)\\s*${esc(p)}\\s*:\\s*([^;]+)`));if(!m)return null;let v=m[1].trim(),important=/\s*!important\s*$/.test(v);return{value:v.replace(/\s*!important\s*$/,'').trim().replace(/\s+/g,' '),important}}function rules(rs,sel,p,val){const x=rs.filter(r=>r.selectors.includes(sel)&&dec(r.body,p)&&(!val||dec(r.body,p).value===val));if(!x.length)throw Error(`rule ${sel} ${p}`);return x}
          const t=parse(parent),a=parse(fs.readFileSync('assets/css/pages/home/mobile-alignment.css','utf8')),manifest=fs.readFileSync('assets/css/pages/home-runtime.css','utf8'),feed=fs.readFileSync('assets/css/pages/home-runtime-feed.css','utf8');if(!(manifest.indexOf('home-runtime-components.css')>=0&&manifest.indexOf('home-runtime-feed.css')>manifest.indexOf('home-runtime-components.css')&&feed.includes('./home/mobile-alignment.css')))throw Error('import order');
          const track='body.home-index-shell #featured-services-track',short='body.home-index-shell #short-videos-track',rail='body.home-index-shell .short-videos .content-rail',scroll='body.home-index-shell #featured-services-track::-webkit-scrollbar';for(const p of ['display','flex-direction','flex-wrap','grid-template-columns','grid-auto-flow','overflow-x','overflow-y','overscroll-behavior-x','scroll-snap-type','-webkit-overflow-scrolling']){const x=dec(rules(t,track,p)[0].body,p),y=dec(rules(a,track,p)[0].body,p);if(!x.important||y.important||x.value!==y.value)throw Error(`track ${p}`)}for(const p of ['display','grid-template-columns','grid-auto-flow','grid-auto-columns','overflow']){const x=dec(rules(t,short,p)[0].body,p),y=dec(rules(a,short,p)[0].body,p);if(!x.important||y.important||x.value!==y.value)throw Error(`short ${p}`)}{const x=dec(rules(t,rail,'overflow','visible')[0].body,'overflow'),y=dec(rules(a,rail,'overflow','visible')[0].body,'overflow');if(!x.important||y.important||x.value!==y.value)throw Error('short rail')} {const x=dec(rules(t,scroll,'display','none')[0].body,'display'),y=dec(rules(a,scroll,'display','none')[0].body,'display');if(!x.important||y.important||x.value!==y.value)throw Error('scrollbar')}
          const fr='body.home-index-shell .featured-services .content-rail',ovs=rules(t,fr,'overflow','hidden').sort((x,y)=>x.order-y.order);if(ovs.length<2||!dec(ovs[0].body,'overflow').important||!dec(ovs.at(-1).body,'overflow').important||ovs.at(-1).order<=ovs[0].order)throw Error('featured winner');const index=fs.readFileSync('index.html','utf8'),classes=[...index.matchAll(/class=["']([^"']*)["']/g)].flatMap(m=>m[1].split(/\s+/).filter(Boolean));const beforeAfterClass=classes.filter(x=>x==='before-after').length,beforeTrack=[...index.matchAll(/id=["']([^"']*)["']/g)].filter(m=>m[1]==='before-after-track').length;if(beforeAfterClass||beforeTrack)throw Error(`before-after exact markup ${beforeAfterClass}/${beforeTrack}`);
          console.log('CASCADE PROOF|removed=18|laterSameKeySameValue=17|sameTargetLaterImportantWinner=1|inactiveBeforeAfterMarkup=0/0|importOrder=components-before-feed|alignmentLoadedLater=1');console.log('BROWSER REQUIRED|value=0|reason=all-active-removed-priorities-have-identical-later-cascade-winners;inactive-co-selectors-have-zero-exact-markup');console.log('RESULT=PASS');
          NODE
      - id: restore
        if: always()
        run: set -euo pipefail; git reset --hard "$TARGET_SHA" >/dev/null; git clean -fdx >/dev/null; test "$(git rev-parse HEAD)" = "$TARGET_SHA"; test "$(git hash-object "$TARGET_FILE")" = "$TARGET_BLOB"; test -z "$(git status --porcelain)"
      - name: Publish
        if: always()
        run: |
          set -euo pipefail; result=FAIL; if test '${{ steps.prove.outcome }}' = success && test '${{ steps.restore.outcome }}' = success; then result=PASS; fi; { echo "RUN_ID=$GITHUB_RUN_ID"; echo "TARGET_SHA=$TARGET_SHA"; echo "PROVE=${{ steps.prove.outcome }}"; echo "RESTORE=${{ steps.restore.outcome }}"; grep -E '^(CANDIDATE IDENTITY|CASCADE PROOF|BROWSER REQUIRED|RESULT=)' /tmp/prove.log || true; echo "RESULT=$result"; if test "$result" = FAIL; then tail -n 260 /tmp/prove.log || true; fi; } >/tmp/evidence.txt; git fetch origin "refs/heads/$DIAG_BRANCH:refs/remotes/origin/$DIAG_BRANCH" >/dev/null; git checkout -B diag-output "origin/$DIAG_BRANCH" >/dev/null; mkdir -p "$(dirname "$RESULT_FILE")"; cp /tmp/evidence.txt "$RESULT_FILE"; git config user.name github-actions[bot]; git config user.email 41898282+github-actions[bot]@users.noreply.github.com; git add "$RESULT_FILE"; git commit -m 'diag: record ux css important 006 static r10 [skip ci]' >/dev/null; git push origin "HEAD:refs/heads/$DIAG_BRANCH" >/dev/null; test "$result" = PASS

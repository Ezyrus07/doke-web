name: 0000 TEMP COM-B02CB Observable Certification and Matrix Reconciliation R2

on:
  pull_request:
    branches: [rep/rep-001-baseline-audit]
    paths:
      - '.github/workflows/0000-temp-com-b02cb-observable-certification-recovery.yml'

permissions:
  contents: write

jobs:
  certify-reconcile-and-finalize:
    if: github.event.pull_request.head.ref == 'com/com-001-baseline-audit'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      CANDIDATE_HEAD: f15737b0e1ef9f82bafb73fe0f594208d9772def
      CANDIDATE_TREE: 0b61f4cfda62994867f9e973b1a7e4209b3cdaab
      PROVEN_STALE_HEAD: f24205b2ae9723ba89c200ccf1065a0c7ea72f84
      PREDECESSOR_HEAD: b3ac2f9e03509ace9c136031566388d7d22d64c7
      PREDECESSOR_TREE: ce909ee45af929445617e8460e3168558cc6fbfb
      B02CB_MODULE_BLOB: 7775c39d4d30f4fdab68c51799f8bc56513d9be9
      B02CB_CONFIG_BLOB: 704670c254ea6eadf2a0b66ea03e788c3276fb34
      B02CB_TEST_BLOB: f6db7fac35a612e16027793a940107b19e56980a
      B02CB_WORKFLOW_BLOB: 765c7dc0f408792cff2e05b6fe3b341cf67b29f0
      B02CA_MODULE_BLOB: ecd88f35ef281aafcd19109f0924787323c3346e
      B02CA_CONFIG_BLOB: 1b564562fe48031448ec07a5f8ba736e0bb7c8fd
      B02BJ_IMPLEMENTATION_BLOB: 99d804721a5d350dcf8e59dd935937e24561e094
      MATRIX_SOURCE_BLOB: b9b2abbee1aed1033c122bf7f2802c7e80844623
      ROUTE_REGISTRY_BLOB: 04a039c1967ffb59502e05584d05831f7c9a1a41
      MODULE_ROUTE_LOADER_BLOB: 85e061fe36339596ba9558a503b2fa7ce02574ae
      COMMUNITY_ROUTE_HANDLERS_BLOB: b7f9f330f6d92c676eb76456b669bdf8dc749c20
      BRANCH: com/com-001-baseline-audit
      TEMP_PATH: .github/workflows/0000-temp-com-b02cb-observable-certification-recovery.yml
      TARGET: backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-entry-container-instance-deterministic-synthetic-invocation-harness.js
      CONFIG: config/com-b02cb-repository-only-continuation-state-registry-storage-backend-entry-container-instance-deterministic-synthetic-invocation-harness.json
      TEST: scripts/test-com-b02cb-repository-only-continuation-state-registry-storage-backend-entry-container-instance-deterministic-synthetic-invocation-harness.js
      WORKFLOW: .github/workflows/com-b02cb-repository-only-continuation-state-registry-storage-backend-entry-container-instance-deterministic-synthetic-invocation-harness.yml
      MATRIX_MD: docs/DOMAIN-COMPLETION-MATRIX.md
      MATRIX_REPORT: reports/generated/domain-completion-matrix-report.json
      TEMP_HEAD_SHA: ${{ github.event.pull_request.head.sha }}

    steps:
      - name: Checkout exact temporary head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TEMP_HEAD_SHA }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Freeze candidate lineage and exact stale-only recovery scope
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TEMP_HEAD_SHA"
          test "$(git rev-parse "$CANDIDATE_HEAD^{tree}")" = "$CANDIDATE_TREE"
          test "$(git rev-parse "$PREDECESSOR_HEAD^{tree}")" = "$PREDECESSOR_TREE"
          git merge-base --is-ancestor "$PROVEN_STALE_HEAD" HEAD
          git merge-base --is-ancestor "$CANDIDATE_HEAD" HEAD
          test "$(git diff --name-only "$PROVEN_STALE_HEAD" HEAD)" = "$TEMP_PATH"
          test "$(git diff --name-only "$CANDIDATE_HEAD" HEAD)" = "$TEMP_PATH"
          expected4="$(printf '%s\n' "$WORKFLOW" "$TARGET" "$CONFIG" "$TEST" | sort)"
          test "$(git diff --name-only "$PREDECESSOR_HEAD" "$CANDIDATE_HEAD" | sort)" = "$expected4"
          test "$(git rev-parse HEAD:$TARGET)" = "$B02CB_MODULE_BLOB"
          test "$(git rev-parse HEAD:$CONFIG)" = "$B02CB_CONFIG_BLOB"
          test "$(git rev-parse HEAD:$TEST)" = "$B02CB_TEST_BLOB"
          test "$(git rev-parse HEAD:$WORKFLOW)" = "$B02CB_WORKFLOW_BLOB"
          test "$(git rev-parse HEAD:backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-entry-container-instance-actual-operation-methods-invocation-readiness.js)" = "$B02CA_MODULE_BLOB"
          test "$(git rev-parse HEAD:config/com-b02ca-repository-only-continuation-state-registry-storage-backend-entry-container-instance-actual-operation-methods-invocation-readiness.json)" = "$B02CA_CONFIG_BLOB"
          test "$(git rev-parse HEAD:backend/shared/http/repository-only-route-continuation-state-registry-storage-backend-entry-container-instance-executable-operation-methods-implementation.js)" = "$B02BJ_IMPLEMENTATION_BLOB"
          test "$(git rev-parse HEAD:config/domain-completion-matrix.json)" = "$MATRIX_SOURCE_BLOB"

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Re-certify B02CB semantics and strict non-invocation
        shell: bash
        run: |
          set -euo pipefail
          node --check "$TARGET"
          node --check "$TEST"
          node "$TEST"
          ! grep -F 'Object.defineProperty(' "$TARGET" "$TEST"
          ! grep -E '(registerOpaqueContinuationState|resolveOpaqueContinuationState|releaseOpaqueContinuationState)[[:space:]]*\(' "$TARGET" "$TEST"
          ! grep -E '\.(call|apply)[[:space:]]*\(|Reflect\.apply[[:space:]]*\(' "$TARGET" "$TEST"
          ! grep -E 'createClient\(|\.rpc\(|\.from\(|fetch\(|process\.env|SUPABASE_SERVICE_ROLE_KEY' "$TARGET" "$TEST"
          grep -F '"kind": "single_use_repository_only_deterministic_synthetic_invocation_harness_materialization"' "$CONFIG"
          grep -F '"singleUse": true' "$CONFIG"
          grep -F '"reusable": false' "$CONFIG"
          ! grep -F 'reusableWithinRepositoryOnlySequence' "$CONFIG" "$TARGET"
          ! grep -E 'executableOperationMethodsInvoked[[:space:]]*:[[:space:]]*true' "$TARGET"
          ! grep -E 'continuationStateStored[[:space:]]*:[[:space:]]*true' "$TARGET"
          ! grep -E 'registryOperationInvoked[[:space:]]*:[[:space:]]*true' "$TARGET"

      - name: Reconcile Matrix only for proven stale-derived blocker
        shell: bash
        run: |
          set -euo pipefail
          npm run write:domain-completion-matrix
          test "$(git rev-parse HEAD:config/domain-completion-matrix.json)" = "$MATRIX_SOURCE_BLOB"
          git diff --quiet -- config/domain-completion-matrix.json
          expected_generated="$(printf '%s\n' "$MATRIX_MD" "$MATRIX_REPORT" | sort)"
          actual_generated="$(git diff --name-only | sort)"
          test "$actual_generated" = "$expected_generated"
          npm run audit:domain-completion-matrix
          npm run audit:agent-governance
          git diff --check

      - name: Commit permanent derived state and self-remove TEMP
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add "$MATRIX_MD"
          git add -f "$MATRIX_REPORT"
          git rm "$TEMP_PATH"
          expected_staged="$(printf '%s\n' "$TEMP_PATH" "$MATRIX_MD" "$MATRIX_REPORT" | sort)"
          actual_staged="$(git diff --cached --name-only | sort)"
          test "$actual_staged" = "$expected_staged"
          git commit -m "ci(com): finalize B02CB repository-only harness certification [run $GITHUB_RUN_ID]"

      - name: Certify permanent B02CB tree
        shell: bash
        run: |
          set -euo pipefail
          expected6="$(printf '%s\n' "$WORKFLOW" "$TARGET" "$CONFIG" "$TEST" "$MATRIX_MD" "$MATRIX_REPORT" | sort)"
          actual="$(git diff --name-only "$PREDECESSOR_HEAD" HEAD | sort)"
          test "$actual" = "$expected6"
          ! git ls-tree -r --name-only HEAD | grep -F "$TEMP_PATH"
          ! git ls-tree -r --name-only HEAD | grep -E '(^|/)temp-com-b02cb-|0000-temp-com-b02cb-'
          test "$(git rev-parse HEAD:$TARGET)" = "$B02CB_MODULE_BLOB"
          test "$(git rev-parse HEAD:$CONFIG)" = "$B02CB_CONFIG_BLOB"
          test "$(git rev-parse HEAD:$TEST)" = "$B02CB_TEST_BLOB"
          test "$(git rev-parse HEAD:$WORKFLOW)" = "$B02CB_WORKFLOW_BLOB"
          test "$(git rev-parse HEAD:config/domain-completion-matrix.json)" = "$MATRIX_SOURCE_BLOB"
          test "$(git rev-parse HEAD:backend/shared/http/route-registry.js)" = "$ROUTE_REGISTRY_BLOB"
          test "$(git rev-parse HEAD:backend/shared/http/module-route-loader.js)" = "$MODULE_ROUTE_LOADER_BLOB"
          test "$(git rev-parse HEAD:backend/modules/communities/route-handlers.js)" = "$COMMUNITY_ROUTE_HANDLERS_BLOB"
          node "$TEST"
          npm run audit:domain-completion-matrix
          npm run audit:agent-governance
          git diff --check "$PREDECESSOR_HEAD" HEAD
          echo "FINAL_HEAD=$(git rev-parse HEAD)"
          echo "FINAL_TREE=$(git rev-parse HEAD^{tree})"

      - name: Fast-forward branch only if TEMP head is still current
        shell: bash
        run: |
          set -euo pipefail
          remote_head="$(git ls-remote origin "refs/heads/$BRANCH" | cut -f1)"
          test "$remote_head" = "$TEMP_HEAD_SHA"
          git push origin "HEAD:refs/heads/$BRANCH"
          pushed_head="$(git ls-remote origin "refs/heads/$BRANCH" | cut -f1)"
          test "$pushed_head" = "$(git rev-parse HEAD)"

name: ANA-001 Post-Hardening Staging Canary (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - name: Certified canary is immutable evidence
        run: |
          echo "Post-hardening ANA canary already certified."
          echo "Canonical evidence: run 35628667088 / job 106429196514."
          echo "Create a new separately authorized workflow for any future staging recertification."

name: COM-B04I Attempt 1 Archived Staging Route Canary

# The exact COM-B04I authorization was consumed by run 31109121586.
# Local conformance failed before repository audit, network access, database
# connection or executor start. This historical workflow is retained as
# evidence and no longer watches the consumed one-shot trigger.
on:
  push:
    branches:
      - com/com-001-baseline-audit
    paths:
      - 'config/com-b04i-attempt-1-archived-never-trigger.json'

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'COM-B04I attempt 1 is archived; a distinct authorization is required.'

name: Doke SCHED-C01D Authenticated Browser Read-Only Canary (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "C01D one-shot consumed; workflow disabled pending deletion."

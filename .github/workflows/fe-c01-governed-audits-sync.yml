name: FE-C01 Governed Audits Sync

on:
  pull_request:
    paths:
      - "assets/js/dev/fe-c01-governed-audits-trigger.txt"

permissions:
  contents: write

concurrency:
  group: fe-c01-governed-audits-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  sync:
    if: github.event.pull_request.number == 19
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout candidate branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Regenerate domain completion matrix
        run: npm run write:domain-completion-matrix

      - name: Validate governed repository audits
        run: |
          npm run audit:domain-completion-matrix
          npm run audit:agent-governance
          npm run audit:unused-asset-candidates
          npm run audit:duplicate-assets
          git diff --check

      - name: Commit deterministic matrix refresh
        shell: bash
        run: |
          if git diff --quiet -- docs/DOMAIN-COMPLETION-MATRIX.md; then
            echo "Domain completion matrix already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/DOMAIN-COMPLETION-MATRIX.md
          git commit -m "FE-C01: refresh domain completion matrix"
          git push origin HEAD:${{ github.head_ref }}

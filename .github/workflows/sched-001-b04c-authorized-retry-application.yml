name: Archived SCHED-B04C Authorized Retry

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "B04C staging authorization consumed; execution disabled."

name: Retired ORD-A07D Deploy Application Reconciler

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reconciler retired
        run: echo 'ORD-A07D deployment reconciliation is complete; this workflow has no write or deployment capability.'

name: Doke UX-CSS-DEBT-012 Anunciar Servico Candidate

on:
  push:
    branches:
      - diag/ux-css-debt-012-anunciar-servico-candidate

permissions:
  contents: read

jobs:
  candidate:
    runs-on: ubuntu-24.04
    env:
      TARGET_SHA: 76b2d52d9beddf03160869c0b07926dc8eb5fdd4
      TARGET_FILE: assets/css/pages/anunciar-servico.css
    steps:
      - name: Checkout exact UX-CSS-DEBT-011 checkpoint
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0

      - name: Guard exact checkpoint
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${TARGET_SHA}"
          git diff --check

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies and Chromium
        shell: bash
        run: |
          set -euo pipefail
          npm ci --ignore-scripts
          node node_modules/@playwright/test/cli.js install --with-deps chromium

      - name: Build deterministic deletion-only candidate
        shell: bash
        run: |
          set -euo pipefail
          cp "${TARGET_FILE}" /tmp/anunciar-parent.css
          node <<'NODE'
          const fs=require('fs'),crypto=require('crypto');
          const file=process.env.TARGET_FILE,text=fs.readFileSync(file,'utf8'),lines=text.split(/\r?\n/);
          let depth=0,selectorParts=[],atRuleParts=[],selector=null;const declarations=[],stack=[];
          const declRe=/^\s*([\w-]+)\s*:\s*(.*?)\s*;\s*$/,norm=parts=>parts.join(' ').replace(/\{\s*$/,'').replace(/\s+/g,' ').trim();
          for(let i=0;i<lines.length;i++){
            const line=lines[i],t=line.trim(),opens=(line.match(/\{/g)||[]).length,closes=(line.match(/\}/g)||[]).length;
            if(!selector){
              if(atRuleParts.length){if(t)atRuleParts.push(t);if(line.includes('{')){stack.push({type:'at',header:norm(atRuleParts),depthBefore:depth});atRuleParts=[];}}
              else if(t.startsWith('@')&&!t.includes(';')){if(line.includes('{'))stack.push({type:'at',header:norm([t]),depthBefore:depth});else atRuleParts=[t];}
              else if(selectorParts.length){if(t)selectorParts.push(t);if(line.includes('{')){selector=norm(selectorParts);selectorParts=[];stack.push({type:'rule',selector,depthBefore:depth});}}
              else if(t&&!t.startsWith('/*')&&!t.startsWith('*')&&t!=='*/'&&t!=='}'&&!t.startsWith('@')&&!line.includes(';')){selectorParts=[t];if(line.includes('{')){selector=norm(selectorParts);selectorParts=[];stack.push({type:'rule',selector,depthBefore:depth});}}
            }
            if(selector){const m=line.match(declRe);if(m){const raw=m[2].trim(),important=/\s*!important\s*$/i.test(raw),context=stack.filter(x=>x.type==='at').map(x=>x.header).join(' || ');declarations.push({lineIndex:i,line:i+1,selector,property:m[1].toLowerCase(),value:raw,important,context});}}
            depth+=opens-closes;while(stack.length&&depth<=stack[stack.length-1].depthBefore){const p=stack.pop();if(p.type==='rule')selector=null;}
          }
          if(atRuleParts.length)throw new Error('unterminated multiline at-rule');
          const last=new Map(),dead=[];for(let i=declarations.length-1;i>=0;i--){const d=declarations[i],key=`${d.context} >>> ${d.selector} >>> ${d.property}`,later=last.get(key);if(later&&(!d.important||later.important))dead.push(d);if(!later||d.important||!later.important)last.set(key,d);}
          const selectors=[...new Set(dead.map(d=>d.selector))].sort(),contexts=[...new Set(dead.map(d=>d.context))].sort(),deadSet=new Set(dead.map(d=>d.lineIndex));
          if(declarations.length!==1033||dead.length!==6||selectors.length!==1)throw new Error(`metric drift ${declarations.length}/${dead.length}/${selectors.length}`);
          if(JSON.stringify(selectors)!==JSON.stringify(['body[data-page="anunciar-servico"] .post-service-availability__toolbar']))throw new Error(`selector drift ${JSON.stringify(selectors)}`);
          if(JSON.stringify(contexts)!==JSON.stringify(['','@media (max-width: 560px)','@media (max-width: 860px)']))throw new Error(`context drift ${JSON.stringify(contexts)}`);
          if((text.match(/!important/gi)||[]).length!==0)throw new Error('unexpected !important');
          const candidate=lines.filter((_l,i)=>!deadSet.has(i)).join('\n');
          fs.writeFileSync('/tmp/anunciar-candidate.css',candidate);fs.writeFileSync('/tmp/anunciar-selectors.json',JSON.stringify(selectors));fs.writeFileSync('/tmp/anunciar-dead.json',JSON.stringify(dead,null,2));fs.writeFileSync(file,candidate);
          const sha=v=>crypto.createHash('sha256').update(v).digest('hex');
          console.log('Deterministic candidate: PASS');console.log(`- declarations: ${declarations.length} -> ${declarations.length-dead.length}`);console.log(`- deleted: ${dead.length}`);console.log(`- selectors: ${selectors.length}`);console.log(`- bytes: ${Buffer.byteLength(text)} -> ${Buffer.byteLength(candidate)}`);console.log(`- parent sha256: ${sha(text)}`);console.log(`- candidate sha256: ${sha(candidate)}`);console.log('- contexts:',JSON.stringify(contexts));console.log('- dead rows:',JSON.stringify(dead));
          NODE
          test "$(git diff --name-only)" = "${TARGET_FILE}"
          test "$(git diff --numstat -- "${TARGET_FILE}" | awk '{print $1}')" = "0"
          test "$(git diff --numstat -- "${TARGET_FILE}" | awk '{print $2}')" = "6"
          git diff --check

      - name: Write canonical runtime parity harness
        shell: bash
        run: |
          cat > /tmp/anunciar-012-capture.js <<'NODE'
          const fs=require('fs'),cp=require('child_process'),{chromium}=require(process.cwd()+'/node_modules/playwright');
          const out=process.argv[2],selectors=JSON.parse(fs.readFileSync('/tmp/anunciar-selectors.json','utf8')),dead=JSON.parse(fs.readFileSync('/tmp/anunciar-dead.json','utf8'));
          const vps=[['1600x1000',1600,1000],['1366x768',1366,768],['1280x802',1280,802],['1024x900',1024,900],['820x1180',820,1180],['608x926',608,926],['390x844',390,844]];
          const server=cp.spawn(process.execPath,['scripts/serve-static-site.js','--host=127.0.0.1','--port=5500'],{stdio:'ignore',env:{...process.env,DOKE_E2E_DISABLE_REMOTE_SERVICES:'1'}}),sleep=ms=>new Promise(r=>setTimeout(r,ms));
          const mediaParts=c=>!c?[]:c.split(' || ').map(x=>x.replace(/^@media\s+/,'').trim());
          async function main(){await sleep(500);const browser=await chromium.launch({headless:true,args:['--no-sandbox','--disable-dev-shm-usage']}),result={},contextCoverage=new Map(dead.map(d=>[d.context,false]));for(const [name,width,height] of vps){const ctx=await browser.newContext({viewport:{width,height},reducedMotion:'reduce'}),page=await ctx.newPage();await page.route('**/*',route=>{const u=new URL(route.request().url()),local=u.origin==='http://127.0.0.1:5500';if(local&&u.pathname.endsWith('.js'))return route.abort();if(local||u.protocol==='data:'||u.protocol==='blob:')return route.continue();return route.abort();});await page.goto('http://127.0.0.1:5500/anunciar-servico.html',{waitUntil:'domcontentloaded'});await page.addStyleTag({content:'*,*::before,*::after{animation:none!important;transition:none!important;caret-color:transparent!important}.doke-document-preloader{display:none!important}html,body{visibility:visible!important;opacity:1!important}'});await page.evaluate(()=>{document.documentElement.removeAttribute('data-auth-guard');document.body.setAttribute('data-page','anunciar-servico');document.querySelectorAll('[data-post-service-guard-ready]').forEach(el=>el.hidden=false);const panel=document.querySelector('[data-step-panel="2"]');if(panel)panel.hidden=false;});
            for(const d of dead){const parts=mediaParts(d.context);const active=await page.evaluate(parts=>parts.every(q=>matchMedia(q).matches),parts);if(active)contextCoverage.set(d.context,true);}
            const canonical=await page.evaluate(selectors=>Object.fromEntries(selectors.map(s=>[s,document.querySelectorAll(s).length])),selectors);if(Object.values(canonical).some(v=>v!==1))throw new Error(`canonical selector coverage drift ${JSON.stringify(canonical)}`);await page.evaluate(()=>document.fonts&&document.fonts.ready);await sleep(60);
            result[name]=await page.evaluate(({selectors,canonical})=>{const clean=v=>String(v??'').replace(/\s+/g,' ').trim(),capture=el=>{const cs=getComputedStyle(el),r=el.getBoundingClientRect(),style={};for(let i=0;i<cs.length;i++){const p=cs[i];style[p]=clean(cs.getPropertyValue(p));}return{tag:el.tagName,id:el.id||'',className:typeof el.className==='string'?clean(el.className):'',rect:[+r.x.toFixed(3),+r.y.toFixed(3),+r.width.toFixed(3),+r.height.toFixed(3)],style};},samples={};for(const s of selectors)samples[s]=[...document.querySelectorAll(s)].map(capture);return{canonical,samples};},{selectors,canonical});console.log(`${name}: canonical selector 1/1`);await ctx.close();}
            for(const [c,hit] of contextCoverage)if(!hit)throw new Error(`at-rule context not exercised: ${c}`);await browser.close();fs.writeFileSync(out,JSON.stringify(result));console.log('All affected selectors and dead at-rule contexts exercised: PASS');}
          main().finally(()=>server.kill('SIGTERM')).catch(e=>{console.error(e);process.exit(1)});
          NODE

      - name: Capture parent runtime and responsive baseline
        shell: bash
        run: |
          set -euo pipefail
          cp /tmp/anunciar-parent.css "${TARGET_FILE}"
          node /tmp/anunciar-012-capture.js /tmp/anunciar-parent-runtime.json
          node scripts/generate-responsive-index-baseline.js
          cp reports/responsive-index-baseline.json /tmp/anunciar-parent-responsive.json
          node scripts/audit-css-responsive-conflicts.js | tee /tmp/anunciar-parent-conflicts.log
          grep -q 'Conflicting class/property pairs: 339' /tmp/anunciar-parent-conflicts.log

      - name: Restore candidate and preserve responsive contract
        shell: bash
        run: |
          set -euo pipefail
          cp /tmp/anunciar-candidate.css "${TARGET_FILE}"
          cp /tmp/anunciar-parent-responsive.json reports/responsive-index-baseline.json
          node scripts/test-responsive-contract.js | tee /tmp/anunciar-responsive.log
          grep -q 'Checks: 893' /tmp/anunciar-responsive.log
          grep -q 'Failures: 0' /tmp/anunciar-responsive.log
          grep -q 'Skips: 277' /tmp/anunciar-responsive.log

      - name: Require exact parent-to-candidate runtime parity
        shell: bash
        run: |
          set -euo pipefail
          node /tmp/anunciar-012-capture.js /tmp/anunciar-candidate-runtime.json
          node <<'NODE'
          const fs=require('fs'),a=JSON.parse(fs.readFileSync('/tmp/anunciar-parent-runtime.json')),b=JSON.parse(fs.readFileSync('/tmp/anunciar-candidate-runtime.json')),diffs=[];
          function walk(x,y,p){if(typeof x!==typeof y||x===null||y===null||typeof x!=='object'){if(x!==y)diffs.push({path:p,parent:x,candidate:y});return;}if(Array.isArray(x)!==Array.isArray(y)){diffs.push({path:p,parent:x,candidate:y});return;}for(const k of [...new Set([...Object.keys(x),...Object.keys(y)])].sort())walk(x[k],y[k],p?`${p}.${k}`:k);}walk(a,b,'');if(diffs.length){console.error(JSON.stringify(diffs.slice(0,100),null,2));throw new Error(`runtime differences ${diffs.length}`);}console.log('Affected-selector computed-style + rect parity: PASS (0 diffs)');
          NODE

      - name: Validate structural and governance contracts
        shell: bash
        run: |
          set -euo pipefail
          node scripts/audit-css-responsive-conflicts.js | tee /tmp/anunciar-candidate-conflicts.log
          grep -q 'Conflicting class/property pairs: 339' /tmp/anunciar-candidate-conflicts.log
          echo 'CSS conflicts: 339 -> 339'
          npm run audit:shared-app-header-contract
          npm run audit:responsive-boundaries
          npm run audit:active-legacy-structures | tee /tmp/anunciar-legacy.log
          npm run audit:domain-completion-matrix
          npm run audit:agent-governance
          npm run audit:duplicate-assets
          grep -q 'active legacy/remediation css: 0' /tmp/anunciar-legacy.log
          grep -q 'active legacy/remediation js: 0' /tmp/anunciar-legacy.log
          grep -q 'high risk active css: 38' /tmp/anunciar-legacy.log

      - name: Require product-only candidate scope
        shell: bash
        run: |
          set -euo pipefail
          git restore -- reports docs/HOME-AUTHORITY-CLASSIFICATION.md docs/PAGE-ASSET-AUTHORITY-MATRIX.md || true
          test "$(git diff --name-only)" = "${TARGET_FILE}"
          test "$(git diff --numstat -- "${TARGET_FILE}" | awk '{print $1}')" = "0"
          test "$(git diff --numstat -- "${TARGET_FILE}" | awk '{print $2}')" = "6"
          git diff --check
          echo 'Candidate scope: PASS (one product file, +0/-6)'

name: FE-J01 Apply Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "FE-J01 controlled apply workflow has been retired."

name: COM-B04I Attempt 2 Archived Unregistered Canary

# GitHub Actions did not register or materialize this branch workflow.
# The consumed authorization cannot be retried from this file.
on:
  push:
    branches:
      - com/com-001-baseline-audit
    paths:
      - 'config/com-b04i-attempt-2-unregistered-archived-never-trigger.json'

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'COM-B04I attempt 2 unregistered workflow is archived.'

name: COM-B03C-R3P Hosted Runtime Observation Harness Readiness

on:
  pull_request:
    branches: [rep/rep-001-baseline-audit]
    paths:
      - 'backend/modules/communities/community-realtime-private-auth-r3p.js'
      - 'config/com-b03c-r3p-hosted-runtime-observation-harness-readiness.json'
      - 'scripts/test-com-b03c-r3p-hosted-runtime-observation-harness-readiness.js'
      - 'docs/validation/COM-B03C-R3P-HOSTED-RUNTIME-OBSERVATION-HARNESS-READINESS.json'
      - '.github/workflows/com-b03c-r3p-hosted-runtime-observation-harness-readiness.yml'

permissions: { contents: read }

jobs:
  certify:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Syntax
        run: |
          node --check backend/modules/communities/community-realtime-private-auth-r3p.js
          node --check scripts/test-com-b03c-r3p-hosted-runtime-observation-harness-readiness.js
      - name: R3P repository synthetic harness
        run: node scripts/test-com-b03c-r3p-hosted-runtime-observation-harness-readiness.js
      - name: Pre-remote hard block
        run: |
          test ! -e config/com-b03c-r3p-hosted-runtime-observation-staging-trigger.json
          node - <<'NODE'
          const c=require('./config/com-b03c-r3p-hosted-runtime-observation-harness-readiness.json');
          for(const value of Object.values(c.prohibitedPreparation)) if(value !== false) process.exit(1);
          const e=require('./docs/validation/COM-B03C-R3P-HOSTED-RUNTIME-OBSERVATION-HARNESS-READINESS.json');
          if(e.authority?.remoteExecution !== false || e.authority?.stagingRead !== false || e.authority?.stagingMutation !== false) process.exit(1);
          if(e.harness?.remoteDependencies !== false || e.harness?.executableSqlPrepared !== false || e.harness?.supabaseClientPrepared !== false || e.harness?.remoteExecutorPrepared !== false) process.exit(1);
          if(e.exactRootCauseProven !== false || e.causalPromotionAllowed !== false) process.exit(1);
          NODE
      - name: Domain Completion Matrix
        run: npm run audit:domain-completion-matrix
      - name: Agent Governance
        run: npm run audit:agent-governance
      - name: Diff hygiene
        run: git diff --check "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}"

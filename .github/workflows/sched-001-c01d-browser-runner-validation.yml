name: Doke SCHED-C01D Browser Runner Validation

on:
  pull_request:
    branches:
      - search/search-ux02-intent-favorites-clean
    paths:
      - 'scripts/run-sched-001-c01d-authenticated-browser-read-only-canary.js'
      - 'scripts/test-sched-001-c01d-authenticated-browser-read-only-canary-runner.js'
      - '.github/workflows/sched-001-c01d-browser-runner-validation.yml'

permissions:
  contents: write

concurrency:
  group: sched-c01d-browser-runner-validation
  cancel-in-progress: true

jobs:
  validate:
    if: >-
      github.event.pull_request.number == 25 &&
      github.event.pull_request.draft == true &&
      github.event.pull_request.head.ref == 'ord/ord-001-baseline-audit'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ord/ord-001-baseline-audit
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install pinned dependencies
        run: npm ci --ignore-scripts

      - name: Validate deterministic browser runner
        run: |
          node --check scripts/run-sched-001-c01d-authenticated-browser-read-only-canary.js
          node --check scripts/test-sched-001-c01d-authenticated-browser-read-only-canary-runner.js
          node scripts/test-sched-001-c01d-authenticated-browser-read-only-canary-runner.js
          node scripts/test-sched-001-c01d-authenticated-browser-read-only-canary.js
          node scripts/run-sched-001-c01d-authenticated-browser-read-only-canary.js --dry-run
          node scripts/audit-sched-001-c01d-authenticated-browser-canary-readiness.js
          node scripts/test-sched-001-c01d-authenticated-browser-canary-readiness.js
          npm run audit:sched-001-c01c-deterministic-frontend-presentation
          npm run test:sched-001-c01c-deterministic-frontend-presentation

      - name: Refresh and validate cumulative matrix
        shell: bash
        run: |
          set -euo pipefail
          npm run write:domain-completion-matrix
          npm run audit:domain-completion-matrix
          changed="$(git diff --name-only | sort)"
          expected="$(printf '%s\n' docs/DOMAIN-COMPLETION-MATRIX.md reports/generated/domain-completion-matrix-report.json | sort)"
          test "$changed" = "$expected"
          git diff --check

      - name: Commit refreshed matrix
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/DOMAIN-COMPLETION-MATRIX.md reports/generated/domain-completion-matrix-report.json
          git diff --cached --quiet && { echo 'Matrix already current'; exit 0; }
          git commit -m 'Refresh domain completion matrix for C01D runner'
          git push origin HEAD:ord/ord-001-baseline-audit

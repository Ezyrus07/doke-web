name: ORD-A10 Blocker Reconcile Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ORD-A10 one-time reconciliation is complete and this workflow is retired."

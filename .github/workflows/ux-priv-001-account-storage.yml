name: Doke UX-PRIV-001 Account Storage

on:
  pull_request:
    paths:
      - 'assets/js/core/account-storage.js'
      - 'assets/js/core/session.js'
      - 'assets/js/pages/news-experience.js'
      - 'scripts/test-ux-priv-001-account-storage.js'
      - 'docs/ux/UX-PRIV-001.md'
      - '.github/workflows/ux-priv-001-account-storage.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ux-priv-001:
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate JavaScript syntax
        run: |
          node --check assets/js/core/account-storage.js
          node --check assets/js/core/session.js
          node --check assets/js/pages/news-experience.js
          node --check scripts/test-ux-priv-001-account-storage.js

      - name: Run UX-PRIV-001 contract tests
        run: node scripts/test-ux-priv-001-account-storage.js

      - name: Preserve auth session contracts
        run: node scripts/audit-auth-session-contracts.js

      - name: Preserve UX-CONT-001 dependency
        run: node scripts/test-ux-cont-001-generation-fences.js

      - name: Preserve UX-CORE-002 dependency
        run: node scripts/test-ux-core-002-mutation-manager.js

      - name: Preserve UX-CORE-001 dependency
        run: node scripts/test-ux-core-001-state-content-registry.js

      - name: Validate patch whitespace
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git diff --check "${{ github.event.pull_request.base.sha }}...${{ github.sha }}"
          else
            git diff --check HEAD^
          fi
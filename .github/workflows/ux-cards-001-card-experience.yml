name: Doke UX-CARDS-001 Card Experience

on:
  pull_request:
    paths:
      - 'assets/js/core/card-experience.js'
      - 'assets/css/core/card-experience.css'
      - 'assets/js/core/page-bootstrap.js'
      - 'assets/js/pages/news-card-pilot.js'
      - 'scripts/test-ux-cards-001-card-experience.js'
      - 'docs/ux/UX-CARDS-001.md'
      - '.github/workflows/ux-cards-001-card-experience.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ux-cards-001:
    runs-on: ubuntu-latest
    timeout-minutes: 18

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate JavaScript syntax
        run: |
          node --check assets/js/core/card-experience.js
          node --check assets/js/core/page-bootstrap.js
          node --check assets/js/pages/news-card-pilot.js
          node --check scripts/test-ux-cards-001-card-experience.js

      - name: Run UX-CARDS-001 contract tests
        run: node scripts/test-ux-cards-001-card-experience.js

      - name: Preserve public service catalog without new debt
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
          else
            BASE_SHA="$(git rev-parse HEAD^)"
          fi

          HEAD_OUTPUT="$(mktemp)"
          BASE_OUTPUT="$(mktemp)"
          HEAD_SIGNATURE="$(mktemp)"
          BASE_SIGNATURE="$(mktemp)"
          NEW_SIGNATURE="$(mktemp)"
          BASE_WORKTREE="$(mktemp -d)/base"

          set +e
          node scripts/test-public-service-catalog-contract.js >"${HEAD_OUTPUT}" 2>&1
          HEAD_STATUS=$?
          git worktree add --detach "${BASE_WORKTREE}" "${BASE_SHA}" >/dev/null 2>&1
          (
            cd "${BASE_WORKTREE}"
            node scripts/test-public-service-catalog-contract.js
          ) >"${BASE_OUTPUT}" 2>&1
          BASE_STATUS=$?
          set -e

          cat "${HEAD_OUTPUT}"

          if [[ ${HEAD_STATUS} -eq 0 ]]; then
            echo "[ux-cards-catalog] Full public catalog contract passes."
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 0
          fi

          if [[ ${BASE_STATUS} -eq 0 ]]; then
            echo "[ux-cards-catalog] Head introduced a catalog failure against a clean base."
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 1
          fi

          grep -E 'AssertionError|assert\(' "${HEAD_OUTPUT}" \
            | sed -E 's#/[A-Za-z0-9_.-]+(/[A-Za-z0-9_.-]+)*/scripts/#scripts/#g' \
            | sort -u >"${HEAD_SIGNATURE}" || true
          grep -E 'AssertionError|assert\(' "${BASE_OUTPUT}" \
            | sed -E 's#/[A-Za-z0-9_.-]+(/[A-Za-z0-9_.-]+)*/scripts/#scripts/#g' \
            | sort -u >"${BASE_SIGNATURE}" || true
          comm -23 "${HEAD_SIGNATURE}" "${BASE_SIGNATURE}" >"${NEW_SIGNATURE}"

          if [[ -s "${NEW_SIGNATURE}" ]]; then
            echo "[ux-cards-catalog] New catalog failure signature introduced by this PR:"
            cat "${NEW_SIGNATURE}"
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 1
          fi

          echo "[ux-cards-catalog] No new catalog failure signature."
          echo "[ux-cards-catalog] Pre-existing stale source assertion remains tracked in issue #64."
          git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true

      - name: Preserve loading baseline without new debt
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
          else
            BASE_SHA="$(git rev-parse HEAD^)"
          fi

          HEAD_OUTPUT="$(mktemp)"
          BASE_OUTPUT="$(mktemp)"
          HEAD_FAILURES="$(mktemp)"
          BASE_FAILURES="$(mktemp)"
          NEW_FAILURES="$(mktemp)"
          BASE_WORKTREE="$(mktemp -d)/base"

          set +e
          node scripts/audit-final-loading-baseline.js >"${HEAD_OUTPUT}" 2>&1
          HEAD_STATUS=$?
          git worktree add --detach "${BASE_WORKTREE}" "${BASE_SHA}" >/dev/null 2>&1
          (
            cd "${BASE_WORKTREE}"
            node scripts/audit-final-loading-baseline.js
          ) >"${BASE_OUTPUT}" 2>&1
          BASE_STATUS=$?
          set -e

          cat "${HEAD_OUTPUT}"

          if [[ ${HEAD_STATUS} -eq 0 ]]; then
            echo "[ux-cards-loading-baseline] Full loading baseline passes."
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 0
          fi

          if [[ ${BASE_STATUS} -eq 0 ]]; then
            echo "[ux-cards-loading-baseline] Head introduced failures against a clean base."
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 1
          fi

          grep '^- ' "${HEAD_OUTPUT}" | sort -u >"${HEAD_FAILURES}" || true
          grep '^- ' "${BASE_OUTPUT}" | sort -u >"${BASE_FAILURES}" || true
          comm -23 "${HEAD_FAILURES}" "${BASE_FAILURES}" >"${NEW_FAILURES}"

          if [[ -s "${NEW_FAILURES}" ]]; then
            echo "[ux-cards-loading-baseline] New failures introduced by this PR:"
            cat "${NEW_FAILURES}"
            git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true
            exit 1
          fi

          echo "[ux-cards-loading-baseline] No new loading failures."
          echo "[ux-cards-loading-baseline] Pre-existing profile debt remains tracked in issue #60."
          git worktree remove --force "${BASE_WORKTREE}" >/dev/null 2>&1 || true

      - name: Preserve UX-PERF-001 dependency
        run: node scripts/test-ux-perf-001-performance-experience.js

      - name: Preserve UX-RESP-001 dependency
        run: node scripts/test-ux-resp-001-responsive-experience.js

      - name: Preserve UX-A11Y-001 dependency
        run: node scripts/test-ux-a11y-001-accessibility-experience.js

      - name: Preserve UX-NAV-001 dependency
        run: node scripts/test-ux-nav-001-overlay-focus.js

      - name: Preserve navigation lifecycle contracts
        run: node scripts/audit-navigation-lifecycle-contract.js

      - name: Preserve auth and page bootstrap contracts
        run: node scripts/audit-auth-session-contracts.js

      - name: Preserve UX-PRIV-001 dependency
        run: node scripts/test-ux-priv-001-account-storage.js

      - name: Preserve UX-CONT-001 dependency
        run: node scripts/test-ux-cont-001-generation-fences.js

      - name: Preserve UX-CORE-002 dependency
        run: node scripts/test-ux-core-002-mutation-manager.js

      - name: Preserve UX-CORE-001 dependency
        run: node scripts/test-ux-core-001-state-content-registry.js

      - name: Validate patch whitespace
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git diff --check "${{ github.event.pull_request.base.sha }}...${{ github.sha }}"
          else
            git diff --check HEAD^
          fi

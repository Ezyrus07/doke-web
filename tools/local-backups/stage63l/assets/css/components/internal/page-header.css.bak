/* Reusable internal page header system.
   Was previously owned from the page layer.
   It now lives in components/internal because the header context cluster is reused by multiple internal screens. */
/* Internal page header system
   Reusable context-label + action cluster for internal views. */

.page-header-context {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
  flex: 0 0 auto;
}

.page-header-context__title {
  display: inline-flex;
  align-items: center;
  margin-right: 4px;
  color: #6b7a90;
  font-size: 1rem;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -0.01em;
  white-space: nowrap;
  flex: 0 0 auto;
}

.page-header-context__action {
  min-height: 44px !important;
  padding: 0 16px;
  gap: 10px;
  white-space: nowrap;
  flex: 0 0 auto !important;
}

.page-header-context__action svg {
  width: 18px;
  height: 18px;
  stroke: currentColor;
  fill: none;
  stroke-width: 1.9;
  stroke-linecap: round;
  stroke-linejoin: round;
  flex: 0 0 auto;
}

.page-header-context__action span {
  display: inline-flex;
  align-items: center;
}

@media (max-width: 1024px) {
  .page-header-context__title {
    display: none;
  }
}

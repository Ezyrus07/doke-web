// detalhe-anuncio.js
// Página zerada intencionalmente. Adicione lógica específica apenas quando a nova estrutura for definida.

(() => {
  const root = document.querySelector('[data-detail-page-root]');
  if (!root) return;
})();

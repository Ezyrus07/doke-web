# Archive

Esta pasta guarda snapshots e material fora do fluxo diário de edição.

- `profilefix-snapshot/` é um retrato antigo do projeto mantido apenas para consulta.
- nada dentro de `archive/` deve ser usado como base oficial de manutenção.

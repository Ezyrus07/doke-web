# Bottom nav rebuild + header avatar fix

- Reconstruído o bottom nav mobile com a classe `doke-bottom-nav`, sem reaproveitar a estrutura antiga.
- Adicionado estado ativo via `aria-current="page"`.
- Bottom nav aparece apenas abaixo de 768px.
- Reforçado o avatar `DK` do header mobile/home para circular, sem `!important`.

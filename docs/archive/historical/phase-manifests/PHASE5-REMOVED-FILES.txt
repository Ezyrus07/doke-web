After applying the Phase 5 patch, remove this old production asset if it remains on disk:

assets/css/pages/pedidos/mobile-longterm-normalization.css

It was replaced by:

assets/css/pages/pedidos/mobile-layout-contract.css

Do not keep both files active; `pedidos.html` must reference only the responsibility-based file.

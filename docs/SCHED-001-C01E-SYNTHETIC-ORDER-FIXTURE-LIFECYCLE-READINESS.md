# SCHED-C01E — Synthetic Order Fixture Lifecycle Readiness

## Purpose

SCHED-C01D reached the authenticated staging read path successfully in run `30753218635`, job `91510829724`:

- two isolated browser contexts authenticated;
- the canonical session, Supabase read provider, order entrypoint and renderer completed;
- two staging reads were observed;
- zero staging mutations were observed;
- both synthetic personas had zero visible order cards.

The remaining blocker is therefore data availability, not browser infrastructure. SCHED-C01E defines a repository-only, fail-closed lifecycle for a bounded synthetic fixture graph that can later support one bound C01D read-only canary.

## Current state

This package is inert.

- staging reads: `0`;
- staging mutations: `0`;
- accounts created: `0`;
- accounts modified: `0`;
- orders created: `0`;
- reservations created: `0`;
- conversations created: `0`;
- payments created: `0`;
- production access: `0`;
- migrations and deployments: `0`;
- merge and auto-merge: disabled.

Generic continuation does not authorize fixture application or browser execution.

## Required dual authorization

A future application package must receive both exact phrases in the same external authorization envelope:

```text
I_EXPLICITLY_AUTHORIZE_SCHED_C01E_SYNTHETIC_ORDER_FIXTURE_LIFECYCLE_ON_DOKE_STAGING
```

```text
I_EXPLICITLY_AUTHORIZE_SCHED_C01D_AUTHENTICATED_BROWSER_READ_ONLY_CANARY_ON_DOKE_STAGING
```

The envelope must bind the two phrase digests, the immutable PR head, the staging project, the two existing synthetic account digests, the generated run ID and the fixture manifest. Neither phrase may be inferred from `proximo` or reused from a previous run.

## Account boundary

C01E must reuse the existing synthetic client and professional accounts already used by C01D.

It may not:

- create an `auth.users` row;
- modify email, password, role, profile or onboarding state;
- use a real account;
- persist raw account identifiers in the repository or evidence.

Only account digests may appear in the sanitized report.

## Bounded fixture graph

Exactly one runtime-generated graph is allowed:

- one synthetic published service;
- one approved service version;
- two orders owned by the same synthetic client/professional pair;
- two order conversations, one per order;
- at most two synthetic message rows;
- one confirmed schedule reservation;
- zero payment, wallet, review or dispute rows.

Every persisted fixture must carry the runtime run ID through both strict metadata markers and an external ID prefix. Raw IDs are generated only at runtime and are never committed.

## Required cases

### Canonical confirmed

One order must be projected through the canonical private projection function:

- order status `scheduled`;
- reservation status `confirmed`;
- `schedule_reservation_id` present;
- `scheduled_at` present;
- reservation and order IDs match;
- reservation `starts_at` equals order `scheduled_at`.

Direct writes to the schedule projection fields are forbidden.

### Alternate authority

The second order must expose `client_intent` or `none`:

- status `requested` or `accepted`;
- no `schedule_reservation_id`;
- no `scheduled_at`;
- client intent, when used, remains metadata only;
- confirmation wording is forbidden.

Both orders must be visible to both synthetic personas before C01D begins.

## Single-workflow lifecycle

A future authorized executor must perform these phases under one immutable head and one run ID:

1. `preflight_zero_residue`;
2. `resolve_existing_synthetic_personas`;
3. `provision_bounded_fixture_graph`;
4. `verify_fixture_visibility`;
5. `execute_bound_c01d_read_only_canary`;
6. `cleanup_fixture_graph` in an unconditional cleanup path;
7. `independent_zero_residue_verification`.

The maximum fixture lifetime is 3600 seconds. Cleanup must run even when provisioning, visibility checks or C01D fail.

## Mutation boundary

Fixture persistence may occur only in a separately authorized service-role application package targeting project `zwkczgewzbsorbrjuzpb`.

The browser remains read-only. After login it may use only `GET`, `HEAD` and `OPTIONS`. Fixture creation, canonical projection and cleanup must never be performed from browser JavaScript.

Production-like targets are rejected. Payments, wallet state, reviews, disputes, billing, infrastructure, Cron and workers remain outside scope.

## Cleanup boundary

Cleanup must verify the double marker before deleting anything. It must abort on a marker mismatch, ambiguous scope or any forbidden dependency.

Bounded fixture relations may include:

- messages and conversations;
- notifications and order status history generated by triggers;
- order and schedule domain events and idempotency rows;
- the confirmed reservation;
- the two orders;
- the service version and service.

The cleanup must fail closed if it finds payments, payment disputes, transactions, receipts, wallet receivables or reviews connected to either fixture order.

Deletion order must remove dependencies before orders and orders before the service. A second, independent query must prove zero residue. Repeating cleanup after success must return an idempotent `already_clean` result.

The existing `public.cleanup_order_canary_run(text)` boundary cannot be reused: it is intentionally restricted to one `ORD-A06` visual-settlement order and rejects broader or multi-order scopes.

## Evidence policy

The final JSON report may contain only:

- digests;
- bounded row counts;
- assertion names and outcomes;
- lifecycle checkpoints;
- cleanup status.

Credentials, raw account IDs, raw fixture IDs, screenshots, video, trace and HTML reports are forbidden.

## Next gate

SCHED-C01E remains blocked until a separate application package is implemented and both exact authorization phrases are supplied for one immutable run. This readiness package does not authorize staging mutation.

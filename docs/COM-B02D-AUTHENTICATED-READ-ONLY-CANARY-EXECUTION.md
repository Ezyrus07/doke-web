# COM-B02D — execução do canário autenticado read-only

Contrato: `com-b02d-authenticated-read-only-canary-execution-v1`.

## Tentativa 1

Autorização recebida:

```text
I_EXPLICITLY_AUTHORIZE_COM_B02D_AUTHENTICATED_READ_ONLY_CANARY_ON_DOKE_STAGING
```

A autorização foi consumida pelo run `31024711149` e não é reutilizável.

O gate de uso único passou, mas a execução foi interrompida na etapa `Audit execution surface`, com a asserção `read-only setting asserted`. A causa foi uma divergência de representação no código-fonte: o executor usa o literal escapado `current_setting(\'transaction_read_only\')`, enquanto o auditor buscava a forma sem escape.

## Alcance efetivo

```text
dependencies installed: true
executor audit started: true
predecessor regressions started: false
database connection attempted: false
database read executed by canary: false
database mutation executed: false
authenticated actor resolved: false
composition root invoked: false
rpc invoked: false
artifact generated by execution: false
```

Nenhuma fixture ou sessão foi criada. Nenhuma rota, runtime ou Edge Function foi implantada. Produção e merge permaneceram bloqueados.

## Executor corrigido

O executor continua preparado para:

1. confirmar PR e projeto staging;
2. selecionar sessão existente válida sem expor identificadores;
3. abrir `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY`;
4. ativar `service_role` apenas localmente;
5. construir o composition root real;
6. invocar somente `com_load_canonical_state_v1`;
7. comparar contagens antes/depois;
8. finalizar com `ROLLBACK`.

O auditor foi corrigido para reconhecer o literal escapado sem enfraquecer nenhuma garantia.

## Retry

Uma segunda tentativa exige nova autorização explícita. A presença da frase neste documento não concede autorização.

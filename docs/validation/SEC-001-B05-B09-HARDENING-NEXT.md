# Next sublot

1. Adapt `order-event-operations` to the shared HTTP security module and define action-sensitive limits.
2. Adapt `quote-template-ai` without weakening its existing short-window and daily AI quotas.
3. Expand the source-closure audit from five to all seven browser-facing functions.
4. Run CI and static contracts.
5. Only after review, apply migration 145 and deploy the seven function bundles to staging.
6. Execute CORS, oversized-body, invalid-JSON, unauthorized, allowed-origin and rate-limit canaries.
7. Enable leaked-password protection and repeat the Supabase Security Advisor.

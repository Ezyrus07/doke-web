#!/usr/bin/env node
const fs=require('fs');
const path=require('path');
const http=require('http');
const crypto=require('crypto');
const { chromium }=require('@playwright/test');
const ROOT=process.cwd();
const TARGET='assets/css/components/shell/mobile-app-shell.css';
const RESULT='.diagnostics/ux-css-important-036-candidate-browser-r2c.json';
const targetPath=path.join(ROOT,TARGET);
const original=fs.readFileSync(targetPath,'utf8');
const candidate=original.replace(/\s*!important\b/g,'');
const count=s=>(s.match(/!important\b/g)||[]).length;
const sha256=s=>crypto.createHash('sha256').update(Buffer.from(s)).digest('hex');
const gitBlob=s=>{const b=Buffer.from(s);return crypto.createHash('sha1').update(Buffer.from(`blob ${b.length}\0`)).update(b).digest('hex');};
const hash=v=>crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex');
const PAGES=['index.html','resultados.html','detalhe-anuncio.html','comunidade.html','perfil-profissional.html','ajuda.html','mensagens.html','pedidos.html','notificacoes.html','configuracoes.html'];
const WIDTHS=[390,560,760,761,1024,1025];
const classTokens=[...new Set([...original.matchAll(/\.([_a-zA-Z]+[_a-zA-Z0-9-]*)/g)].map(m=>m[1]))].sort();
const out={boundary:'UX-CSS-IMPORTANT-036',proof:'candidate-browser-r2c',certified035Head:'483d49d5f59c2eb24be0169f3d7ed6aec9703679',target:TARGET,parentBlobExpected:'af6fd982517f2bc821435e633d70237e95ee11a8',parentImportant:count(original),candidateImportant:count(candidate),candidateSha256:sha256(candidate),candidateGitBlob:gitBlob(candidate),candidateBytes:Buffer.byteLength(candidate),selectorClassTokenCount:classTokens.length,pages:PAGES,widths:WIDTHS,statesPerPhase:PAGES.length*WIDTHS.length,criteriaRelaxed:false,syntheticDom:false,credentialsUsed:false,externalNetworkBlocked:true,readiness:'embedded-node-http-listen + commit + body-attached + deterministic-settle',historicalR2Preserved:true,historicalR2BPreserved:true,parent:null,candidate:null,diffs:null,status:'RUNNING'};
function assert(c,m){if(!c)throw new Error(m)}
assert(count(original)===314,`expected 314 parent markers, got ${count(original)}`);
assert(count(candidate)===0,`candidate markers not zero: ${count(candidate)}`);
function mime(p){if(p.endsWith('.css'))return'text/css';if(p.endsWith('.js'))return'application/javascript';if(p.endsWith('.json'))return'application/json';if(p.endsWith('.svg'))return'image/svg+xml';if(p.endsWith('.png'))return'image/png';if(p.endsWith('.jpg')||p.endsWith('.jpeg'))return'image/jpeg';if(p.endsWith('.webp'))return'image/webp';return'text/html';}
async function startServer(){return await new Promise((resolve,reject)=>{const server=http.createServer((req,res)=>{try{const u=new URL(req.url,'http://127.0.0.1');let rel=decodeURIComponent(u.pathname).replace(/^\/+/, '')||'index.html';const full=path.resolve(ROOT,rel);if(!full.startsWith(ROOT)){res.writeHead(403);return res.end('forbidden')}const stat=fs.existsSync(full)&&fs.statSync(full);const file=stat&&stat.isDirectory()?path.join(full,'index.html'):full;if(!fs.existsSync(file)){res.writeHead(404);return res.end('not found')}res.writeHead(200,{'Content-Type':mime(file),'Cache-Control':'no-store'});fs.createReadStream(file).pipe(res);}catch(e){res.writeHead(500);res.end(String(e));}});server.once('error',reject);server.listen(0,'127.0.0.1',()=>resolve({server,port:server.address().port}));});}
async function phase(label,css,port){fs.writeFileSync(targetPath,css);const browser=await chromium.launch({headless:true});const states=[];try{for(const pageName of PAGES){for(const width of WIDTHS){const context=await browser.newContext({viewport:{width,height:900},deviceScaleFactor:1});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(String(e.message||e)));await page.route('**/*',async route=>{const u=route.request().url();if(u.startsWith(`http://127.0.0.1:${port}/`)||u.startsWith('data:')||u.startsWith('blob:'))await route.continue();else await route.abort();});await page.addInitScript(()=>{let seed=123456789;Math.random=()=>((seed=(1103515245*seed+12345)>>>0)/4294967296);});await page.goto(`http://127.0.0.1:${port}/${pageName}`,{waitUntil:'commit',timeout:15000});await page.waitForSelector('body',{state:'attached',timeout:10000});await page.addStyleTag({content:'*,*::before,*::after{animation:none!important;transition:none!important;caret-color:transparent!important;scroll-behavior:auto!important}'});await page.waitForTimeout(900);const meta=await page.evaluate(tokens=>{const targetNeedle='/assets/css/components/shell/mobile-app-shell.css';const loaded=Array.from(document.styleSheets).map(s=>s.href||'').some(h=>h.includes(targetNeedle));const set=new Set(tokens);const els=Array.from(document.querySelectorAll('*')).filter(el=>Array.from(el.classList||[]).some(c=>set.has(c)));const pth=el=>{const parts=[];let n=el;while(n&&n.nodeType===1&&n!==document.documentElement){let i=1,s=n;while((s=s.previousElementSibling))i++;parts.push(`${n.tagName.toLowerCase()}:nth-child(${i})`);n=n.parentElement;}return parts.reverse().join('>');};const nodes=els.map(el=>{const cs=getComputedStyle(el);const props=Array.from(cs).sort();const style=props.map(p=>`${p}:${cs.getPropertyValue(p)}`).join(';');const r=el.getBoundingClientRect();return{path:pth(el),classes:Array.from(el.classList).sort(),rect:[r.x,r.y,r.width,r.height].map(x=>Math.round(x*1000)/1000),style};}).sort((a,b)=>a.path.localeCompare(b.path));return{href:location.pathname,bodyClasses:Array.from(document.body.classList).sort(),loaded,nodeCount:nodes.length,nodes,scroll:[document.documentElement.scrollWidth,document.documentElement.scrollHeight],viewport:[innerWidth,innerHeight]};},classTokens);const normalized={...meta,nodes:meta.nodes.map(n=>({path:n.path,classes:n.classes,rect:n.rect,styleHash:hash(n.style)}))};const s1=await page.screenshot({fullPage:true,animations:'disabled'});await page.waitForTimeout(150);const s2=await page.screenshot({fullPage:true,animations:'disabled'});const h1=crypto.createHash('sha256').update(s1).digest('hex'),h2=crypto.createHash('sha256').update(s2).digest('hex');states.push({page:pageName,width,metadataHash:hash(normalized),screenshotHash:h2,screenshotStable:h1===h2,targetLoaded:meta.loaded,nodeCount:meta.nodeCount,pageErrors:errors,href:meta.href});await context.close();}}}finally{await browser.close();}return{label,states,stableScreenshots:states.filter(s=>s.screenshotStable).length,targetLoadedStates:states.filter(s=>s.targetLoaded).length,pageErrorCount:states.reduce((n,s)=>n+s.pageErrors.length,0)};}
(async()=>{const {server,port}=await startServer();try{out.parent=await phase('parent',original,port);out.candidate=await phase('candidate',candidate,port);const pm=new Map(out.parent.states.map(s=>[`${s.page}@${s.width}`,s]));const diffs=[];for(const c of out.candidate.states){const k=`${c.page}@${c.width}`,p=pm.get(k);if(!p){diffs.push({key:k,type:'missing-parent'});continue;}if(p.metadataHash!==c.metadataHash)diffs.push({key:k,type:'metadata'});if(p.screenshotHash!==c.screenshotHash)diffs.push({key:k,type:'screenshot'});if(p.href!==c.href)diffs.push({key:k,type:'route'});if(p.targetLoaded!==c.targetLoaded)diffs.push({key:k,type:'target-load'});}out.diffs={total:diffs.length,metadata:diffs.filter(d=>d.type==='metadata').length,screenshot:diffs.filter(d=>d.type==='screenshot').length,route:diffs.filter(d=>d.type==='route').length,targetLoad:diffs.filter(d=>d.type==='target-load').length,sample:diffs.slice(0,50)};const total=out.statesPerPhase;assert(out.parent.states.length===total&&out.candidate.states.length===total,'state coverage incomplete');assert(out.parent.stableScreenshots===total&&out.candidate.stableScreenshots===total,'screenshot instability detected');assert(out.parent.pageErrorCount===0&&out.candidate.pageErrorCount===0,'page errors detected');assert(out.diffs.total===0,`A/B differences detected: ${JSON.stringify(out.diffs)}`);out.status='BROWSER_PASS';}catch(e){out.status='FAIL';out.error=String(e&&e.stack||e);process.exitCode=1;}finally{fs.writeFileSync(targetPath,original);await new Promise(r=>server.close(r));fs.mkdirSync(path.dirname(RESULT),{recursive:true});fs.writeFileSync(RESULT,JSON.stringify(out,null,2)+'\n');}})();

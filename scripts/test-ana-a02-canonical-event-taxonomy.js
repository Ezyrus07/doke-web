'use strict';
const t=require('../backend/modules/analytics/canonical-event-taxonomy');const checks=[];const check=(n,v)=>checks.push({name:n,passed:Boolean(v)});
check('unknown rejected',t.validateClientSubmission('made.up',{}).code==='ANA_EVENT_UNKNOWN');check('order browser rejected',t.validateClientSubmission('order.completed',{}).code==='ANA_EVENT_SERVER_AUTHORITY_REQUIRED');check('payment browser rejected',t.validateClientSubmission('payment.held',{}).code==='ANA_EVENT_SERVER_AUTHORITY_REQUIRED');check('raw query rejected',t.validateClientSubmission('service.detail_viewed',{raw_query:'x'}).code==='ANA_EVENT_SENSITIVE_PAYLOAD_REJECTED');check('impression proof required',t.validateClientSubmission('search.result_impression',{}).code==='ANA_EXPOSURE_PROOF_REQUIRED');check('impression accepted',t.validateClientSubmission('search.result_impression',{exposureProof:'proof'}).ok===true);check('quote order required',t.validateClientSubmission('quote.submitted',{}).code==='ANA_ORDER_REFERENCE_REQUIRED');check('quote submit accepted',t.validateClientSubmission('quote.submitted',{orderId:'order-1'}).ok===true);check('budget alias semantic',t.resolveEventName('service_metric_events.budget')==='service.budget_cta_clicked');check('repeat derived',t.EVENTS['repeat.order_completed'].eventClass===t.EVENT_CLASSES.DERIVED_PROJECTION&&!t.EVENTS['repeat.order_completed'].clientEmitAllowed);
const failed=checks.filter((x)=>!x.passed).map((x)=>x.name);console.log(JSON.stringify({taxonomyVersion:t.TAXONOMY_VERSION,total:checks.length,passed:checks.length-failed.length,failed:failed.length,status:failed.length?'failed':'passed',failedCases:failed},null,2));if(failed.length)process.exitCode=1;
